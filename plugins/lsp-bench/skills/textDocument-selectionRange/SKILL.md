---
name: textDocument/selectionRange
description: YAML for `textDocument/selectionRange`. Returns nested ranges (inner-most token to outer-most enclosing block).
---

# bench `textDocument/selectionRange`

```yaml
methods:
  textDocument/selectionRange: {}
```

Cursor on any token — deeper nesting yields more interesting chains.
